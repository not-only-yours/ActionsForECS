resource "aws_route53_record" "www" {
  zone_id = "${aws_route53_zone.primary.zone_id}"
  name    = "monitoring-ops.pp.ua"
  type    = "A"
  ttl     = "300"

  alias {
    name                   = "${aws_alb.alb-frontend.dns_name}"
    zone_id                = "Z02688093SHEKBL66QRDQ"
    evaluate_target_health = true
  }
}